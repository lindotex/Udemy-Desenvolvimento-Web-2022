// jQuery => On and Off Event Commands

$(document).on()
$(document).off()

$(document).live()      // Descontinuado na versão 3 do jQuery
$(document).delegate()  // Descontinuado na versão 3 do jQuery
$(document).bind()      // Descontinuado na versão 3 do jQuery
